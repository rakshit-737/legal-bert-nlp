streamlit
# Core ML stack (let pip pick versions compatible with Streamlit Cloud's Python)
torch
transformers
sentence-transformers
accelerate
datasets
evaluate
# Utilities
python-dotenv
tqdm
# Optional/feature dependencies
pandas
numpy
matplotlib
seaborn
pdfplumber
pytesseract
# scikit-learn is only used for TF-IDF fallback + some eval scripts; avoid it on Streamlit Cloud
# because it may require compilation under newer Python versions.
# scikit-learn
